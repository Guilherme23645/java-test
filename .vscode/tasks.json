{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Clear",
            "type": "shell",
            "command": "${command:workbench.action.terminal.clear}",
            "presentation": {
                "reveal": "silent",
                "panel": "dedicated"
            }
        }
    ]
}